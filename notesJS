== сравнение с приведением типов
=== сравнение без приведения типов
----------
var string = ""; - пустая строка

----------
объекты
var name = {
    key : "value",
    key2 : "value2"
};

литерал объекта - фигурные скобки и пары ключ-значение.
//литерал - запись в исходном коде программы, представляющее собой фиксированное значение.

int number = 1;
number - переменная,
1 - литерал.

ключ - строка.
если в ключе есть пробелы, записывается в двойных кавычках.

name["key"];
name.key;

Object.keys(name); возвращает массив ключей.

Ключи не упорядочиваются и выводятся в случайном порядке.

добавление ключей
name["key"] = 1;
name.key = 1;

несуществующий ключ - undefined

---

массивы объектов

доступ последовательно через имя, индекс, свойство.
------------------------

HTML - HyperText Markup Language язык гипертекстовой разметки

  /\ /\
=( o.o )=
  )   (  //
 (__ __)//

у тегов - атрибуты.

title - описание при наведении.

Условные конструкции и циклы.

Условная конструкция - if, if else

if (условие){
    тело if 
}

if (условие){
    тело if 
} else {
    тело else
}

if (условие){
    тело if 
} else if(условие){
    тело else if
}

если
var condition = true;
можно if (condition){}

--------
циклы

break - выход из цикла

while(условие){
    тело цикла
}
---
for( настройка; условие; приращение){
for( var i = 0;  i < 10: i++       ){
    тело цикла
}

i < array.length

---------

var name = prompt("text");
OK - возвращает введенную строку или
Cancel - возвращает null
далее null может неявно приводиться в "null"

confirm("text") возвращает булевые значения

alert("text") оповещение

-------------

Функции

функциональное выражение:
var name = function(){
    тело функции
};

name(); - вызов функции

возвращаемое значение будет undefined
значение возвращается через return 
при любом ретёрне производится незамедлительный выход из функции
пустой ретёрн возвращает undefined


var name = function(){
    тело функции
    return "возвращаемое значение";
};

var name = function(аргумент){
    тело функции
};

короткая запись
объявление функции:

function name(args){
    тело
}

Math.abs() - АБСолютное значение числа

хеш-функция - функция, вычисляющая хеш, чтобы потом положить элемент в хеш-таблицу 
    по индексу хеш.

Назначение простой хэш-функции:
    прием значения;
    выполнение циклов через каждый символ;
    возвращение кода символа для каждого символа;
    умножение его на индекс;
    добавление его к исходному хэш-значению;
    использование оператора остатка для непревышения длины размера массива.

